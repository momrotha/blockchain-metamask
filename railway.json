{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd my-fastapi-app && uvicorn main:app --host 0.0.0.0 --port $PORT"
  }
}
